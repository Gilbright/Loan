{% extends 'base.html.twig' %}
{% block title %} {{ user.firstName}} {% endblock %}
{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-md-6 ">
                <table class="table table-borderless" >
                    <tbody>
                    <tr>
                        <td>First Name</td>
                        <td>{{ user.firstName }}</td>
                    </tr>
                    <tr>
                        <td>Last Name</td>
                        <td>{{ user.lastName }}</td>
                    </tr>
                    <tr>
                        <td>Email</td>
                        <td>{{ user.email }}</td>
                    </tr>
                    </tbody>
                </table>
            </div>


        </div>
    </div>
    {#
    <a href="#" onclick="event.preventDefault(); confirm('Are you sure you want to delete?') && document.getElementById('js-pin-delete').submit();">Delete My Account</a>

    <form id="js-pin-delete"  action="{{ path('app_delete',{ 'id': pin.id}) }}" method="post" style="display: none;">
        <input type="hidden"  name="token" value="{{ csrf_token('pin_deletion_' ~ pin.id) }}">
        <input type="hidden" name="_method" value="DELETE">
    </form>
    #}

{% endblock %}