{% extends 'base.html.twig' %}
{% block title %} Edit pin {% endblock %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}
{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-md-6 mx-auto" >
                <article>
                    <h2> Edit Pin #{{ pin.id}} </h2>
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <br>
                    <input type="submit" value="edit pin" class="btn btn-primary btn-lg btn-block" formnovalidate>
                    {{ form_end(form) }}

                </article>
            </div>
        </div>
    </div>

{% endblock %}